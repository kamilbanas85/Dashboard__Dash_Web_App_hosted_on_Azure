pip install numpy
pip install scipy
pip install pandas

pip install dash
pip install plotly
pip install dash-extensions --use-feature=2020-resolver

pip install pyodbc
pip install retry

# pip install feather-format


